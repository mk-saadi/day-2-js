// number type variable
var sunglassPrice=120;

// string type variable

var specialoneName="CC";

// boolean type variable

var istaken=true;

// 2 types of number variable: interger(120), float(1.20, 4.5)
var sunglass1Price=1.20;

// variable naming convention rules: best practice

/**
 * 1. single word
 * 2. no gap or space
 * 3. no quote
 * 4. no keyword
 * 5. can not start with a number
 * 6. should not contain -
 * 7. should use camelCase
 */

// math operation: +, -, *, /
// shorthand: +=, -=, ++, --

// convert: parseInt, parseFloat

// toFixed

// reminder: vagses, vagfol